# Single compose for dev and tests (PCOV in Dockerfile for coverage)
name: sepa-payment-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sepa-payment-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: ["tail", "-f", "/dev/null"]
    networks:
      - sepa-payment-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  sepa-payment-bundle-network:
    driver: bridge
