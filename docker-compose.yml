services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sepa-payment-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - XDEBUG_MODE=coverage
    command: ["tail", "-f", "/dev/null"]
    networks:
      - sepa-payment-bundle-network

  # Service for running tests
  test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sepa-payment-bundle-test
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - XDEBUG_MODE=coverage
    command: ["tail", "-f", "/dev/null"]
    networks:
      - sepa-payment-bundle-network
    profiles:
      - test

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  sepa-payment-bundle-network:
    driver: bridge

