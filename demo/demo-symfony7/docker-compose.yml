services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sepa-payment-demo-symfony7-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: php-fpm
    networks:
      - sepa-payment-demo-network

  nginx:
    image: nginx:alpine
    container_name: sepa-payment-demo-symfony7-nginx
    ports:
      - "${PORT:-8001}:80"
    volumes:
      - ./public:/var/www/html/public:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    networks:
      - sepa-payment-demo-network

volumes:
  composer-cache:
    driver: local

networks:
  sepa-payment-demo-network:
    driver: bridge

